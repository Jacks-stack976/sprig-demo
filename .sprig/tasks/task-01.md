# Task 01 — line total helper

Add an exported function `lineTotal(quantity: number, unitPrice: number): number`
to `src/invoice.ts`. It returns `quantity * unitPrice` rounded to two decimals
using the existing rounding helper. Do not hand-roll rounding.
