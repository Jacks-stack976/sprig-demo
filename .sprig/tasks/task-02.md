# Task 02 — flat discount

Add an exported function `applyFlatDiscount(amount: number, flat: number): number`
to `src/discount.ts`. Subtract `flat` from `amount`, never returning below 0,
rounded to two decimals. Throw if `flat` is negative.
