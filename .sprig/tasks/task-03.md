# Task 03 — euro formatting

Add an exported function `formatEUR(amount: number): string` to `src/currency.ts`
that formats an amount as euros using Intl.NumberFormat (locale "de-DE",
currency "EUR"), rounding via the existing helper first.
