# Task 004 — quantity guard

Add an exported function `isValidQuantity(n: number): boolean` to
`src/validate.ts`.

Return `true` only for positive integers — reject zero, negatives, and any
value with a fractional part.
