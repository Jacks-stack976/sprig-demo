# Task 001 — split a total into even shares

Add an exported function `splitEvenly(total: number, parts: number): number[]`
to `src/invoice.ts`.

It divides `total` into `parts` as-equal-as-possible shares and returns them as
an array (one entry per share). Throw if `parts` is less than 1.
